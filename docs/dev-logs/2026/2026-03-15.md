# 2026-03-15 Dev Log

## Current checkpoint
- `git status` is clean on `main`, and `main` is synchronized with `origin/main`.
- There is no `docs/` directory in the repo yet; this file is the first log entry inside `docs/dev-logs/2026/`.
- A screenshot of the PR list (red status for #17–#21, #20, #19, #18, and #17) shows they were closed or blocked. I could not verify the implementations from this workspace; treat them as still needing work.

## Blockers carried forward
1. Stabilize `switchActiveTab` so it only updates when the handler identity changes by wrapping it with `useCallback`.
2. Adjust `src/App.shortcut.test.jsx` to reflect the current single “Switch to Untitled” tab, since the test still assumes multiple duplicates.
3. Rerun `npm run check` inside `src/ghost-writer-editor` (the embedded renderer/editor workspace) and fix any remaining failures so the suite can turn green.
4. Confirm exactly what features were meant to be delivered by the red PRs (#17 “Prevent duplicate file tabs”, #18 “Improve print page break formatting”, #19 “Improve section-aware print pagination”, #20 “Fix empty checkbox preview rendering”, and #21 “Fix duplicate tab reopening stale tabs”). They are currently marked closed/blocked in the UI but not evidently implemented in this branch; review commit history or PR descriptions to capture their requirements before coding.

## Next steps when work resumes
- Implement the missing behavior from the red PRs, tackling one change per PR (respecting the “do it safely and one by one” instruction).
- Wrap up `switchActiveTab` and the shortcut test update, then rerun `npm run check` in `src/ghost-writer-editor`.
- Add or update any docs that explain the new behaviors, then `git add`/`git commit`.
- Pushing is currently blocked locally because `git push origin main` needs a GitHub PAT or authenticated credential helper; set that up before opening a PR.

## Status for handoff
- Remaining work is bigger than one command; the repo is safe to leave clean now.
- Tell the next agent to start from this log, keep the repository on `main`, and resume from the numbered blockers above.
